params {
  // Defaults only, expecting to be overwritten
  max_memory = 20.GB
  max_cpus = 22
  max_time = 48.h
}

process {
  executor = 'local'
}

executor {
  $local {
    cpus = 22
    memory = '28 GB'
  }
}