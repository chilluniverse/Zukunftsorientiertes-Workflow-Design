params
{
  max_memory = 20.GB
  max_cpus = 22

  noDocker = false

  align = false
  DEG = false
  dmel = false
  dmau = false

  peakcalling = false
}

executor {
  $local {
    cpus = 22
    memory = '30 GB'
  }
}

if (params.noDocker) {
  docker.enabled = false
} else {
  docker.enabled = true
}